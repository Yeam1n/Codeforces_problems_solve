#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    while (n--) {
        string s, y, z;
        cin >> s >> y >> z;
        cout << s[0] << y[0] << z[0] << '\n';
    }
    return 0;
}
